#!/usr/bin/python3
""" Module imported """


def is_same_class(obj, a_class):
    """ The function definition """
    if (type(obj) is a_class):
        return (True)
    else:
        return (False)
