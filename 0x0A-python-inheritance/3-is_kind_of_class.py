#!/usr/bin/python3
""" Module imported """


def is_kind_of_class(obj, a_class):
    """ The function definition """
    if (isinstance(obj, a_class)):
        return (True)
    else:
        return (False)
